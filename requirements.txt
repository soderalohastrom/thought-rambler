# Vercel serverless function requirements
# Core API dependencies (lightweight for serverless deployment)
requests==2.31.0
fastapi==0.104.1
pydantic==2.5.0
python-dotenv==1.0.0

# Note: Heavy ML dependencies (transformers, torch, spacy) removed due to Vercel size limits
# LLM enhancement will be implemented in a future iteration with external API calls
# For now, using rule-based parsing with basic NLP functionality